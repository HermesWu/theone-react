
import * as React from 'react'

interface IconProps{
    name: string
}

const icon:React.FunctionComponent<IconProps> = (props) => {
    return (
        <div>
            icon
        </div>
    )
}

export default icon